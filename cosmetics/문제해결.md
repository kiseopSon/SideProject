# 🔧 백엔드 서버 실행 문제 해결

## 문제 상황

백엔드 서버가 실행되지 않거나, `Python`만 출력되고 실행이 안되는 경우

**백엔드 디렉토리(`backend`)에 `venv` 폴더가 있다면 가상환경을 활성화해야 합니다!**

---

## ✅ 해결 방법 (가장 간단)

### 방법 1: start-server.bat 사용 (권장)

백엔드 디렉토리에서:
```powershell
cd C:\Users\icaru\git\SideProject\cosmetics\backend
start-server.bat
```

이 스크립트가 자동으로:
1. 가상환경 활성화
2. 필수 패키지 확인 및 설치
3. 서버 시작

---

### 방법 2: 가상환경 활성화 후 실행

**1단계: 백엔드 디렉토리로 이동**
```powershell
cd C:\Users\icaru\git\SideProject\cosmetics\backend
```

**2단계: 가상환경 활성화**
```powershell
venv\Scripts\activate
```

**활성화되면 프롬프트 앞에 `(venv)`가 표시됩니다:**
```
(venv) C:\Users\icaru\git\SideProject\cosmetics\backend>
```

**3단계: 서버 실행**
```powershell
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8500
```

---

### 방법 3: 가상환경 없이 실행 (전역 Python 사용)

가상환경을 사용하지 않고 전역 Python을 사용하려면:

```powershell
cd C:\Users\icaru\git\SideProject\cosmetics\backend
python -m pip install -r requirements.txt
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8500
```

---

## 🐛 일반적인 오류 및 해결

### 오류 1: `python: command not found` 또는 `Python`만 출력
**원인**: Python이 PATH에 없거나 설치되지 않음

**해결:**
1. `py --version` 시도 (Windows Python Launcher)
2. Python 재설치: [Python 공식 사이트](https://www.python.org/downloads/)
   - 설치 시 "Add Python to PATH" 옵션 체크

---

### 오류 2: `ModuleNotFoundError: No module named 'fastapi'`
**원인**: 필수 패키지가 설치되지 않음

**해결:**
```powershell
cd backend
venv\Scripts\activate  # 가상환경 활성화
pip install -r requirements.txt
```

---

### 오류 3: `'venv' is not recognized as an internal or external command`
**원인**: 가상환경이 생성되지 않았거나 다른 경로에 있음

**해결:**
1. `backend` 디렉토리에 `venv` 폴더가 있는지 확인
2. 없으면 가상환경 생성:
   ```powershell
   cd backend
   python -m venv venv
   venv\Scripts\activate
   pip install -r requirements.txt
   ```

---

### 오류 4: `Address already in use` (포트 8500 사용 중)
**원인**: 다른 프로그램이 8500 포트를 사용 중

**해결:**
1. 다른 터미널에서 실행 중인 서버 종료 (Ctrl+C)
2. 또는 다른 포트 사용:
   ```powershell
   python -m uvicorn main:app --reload --host 127.0.0.1 --port 8501
   ```

---

## 📋 빠른 체크리스트

- [ ] `backend` 디렉토리에 있는가?
- [ ] 가상환경이 활성화되어 있는가? (프롬프트에 `(venv)` 표시)
- [ ] Python이 실행되는가? (`python --version`)
- [ ] 필수 패키지가 설치되어 있는가? (`pip list`)
- [ ] `main.py` 파일이 존재하는가?
- [ ] 포트 8500이 사용 가능한가?

---

## 🎯 단계별 실행 가이드

### 가장 확실한 방법

**터미널 1개 열고:**
```powershell
# 1. 백엔드 디렉토리로 이동
cd C:\Users\icaru\git\SideProject\cosmetics\backend

# 2. 가상환경 활성화
venv\Scripts\activate

# 3. 서버 실행
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8500
```

**정상 실행되면:**
```
INFO:     Uvicorn running on http://127.0.0.1:8500 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Application startup complete.
```

---

### start-server.bat 사용 (가장 간단)

```powershell
cd C:\Users\icaru\git\SideProject\cosmetics\backend
start-server.bat
```

---

## 💡 팁

1. **가상환경 사용이 좋은 이유**
   - 프로젝트별로 패키지 버전 관리 가능
   - 전역 Python 환경 오염 방지
   - 다른 프로젝트와 충돌 방지

2. **서버 실행 확인**
   - 브라우저에서 `http://localhost:8500` 접속
   - 또는 `http://localhost:8500/docs` 접속 (API 문서)

3. **오류 메시지 확인**
   - 터미널에 표시되는 전체 오류 메시지를 확인하세요
   - 오류 메시지가 문제 해결의 핵심입니다

4. **가상환경 비활성화**
   - 서버 종료 후: `deactivate` 명령어 사용
